rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public: read only for menuItems
    match /menuItems/{itemId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
